<?php echo \Hook\Hook\Hook::run('displayFoot');?>
<script><?php
$data = [];
foreach (apcu_fetch('global')['regexp']['core'] as $key => $value) {
    $data[] = $key.':/'.$value.'/';
}
echo 'var config = {regexp:{',join(',', $data),'}};';
?></script>
</body>
</html>